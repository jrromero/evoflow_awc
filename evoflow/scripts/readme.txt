- "launch_evoflow.sh" is used to execute evoflow. It receives a configuration file indicating the evolutionary parameters

- "top10_ensemble.py" and "top10W_ensemble.py" use the results obtained by "launch_evoflow.sh" to build an ensemble only considering the accuracy of the individuals
